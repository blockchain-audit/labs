use debug::PrintTrait;

fn main() {
    let is_awesom = true;
    let version: u8 = 2;

    if is_awesom && version > 0{
        'Lets, code'.print();
    }
    else {
        'Try,agen'.print();
    }
}