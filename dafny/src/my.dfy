        method {:tes} m() {
        print(10);
        print("\n");
        print(2);
    }

// 29/05/2024 
//לכתוב קלט של מספרים בפונקציה wdiv

        x := 3
        y := 5
        wDiv(3 * wid, 5 * wid)

        wDiv(x, y): (r)
        {
        m := 3000000 // x * wid
        w := 2500 // y / 2
        z := 3002500 // m + w
        3002500 / 5000 := 600// (r) z / y
    }